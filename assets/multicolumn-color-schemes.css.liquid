/* stylelint-disable */
/* Multicolumn-specific color scheme mappings generated from settings.color_schemes (color_scheme_group) */
{% assign schemes = 'scheme-1,scheme-2,scheme-3,scheme-4,scheme-5' | split: ',' %}
{% for scheme_id in schemes %}
  {% assign scheme = settings.color_schemes[scheme_id] %}
  {% if scheme != blank %}
.multicolumn.color-scheme-{{ scheme_id }} {
  --color-foreground: {{ scheme.settings.text | color_extract: 'red' }}, {{ scheme.settings.text | color_extract: 'green' }}, {{ scheme.settings.text | color_extract: 'blue' }};
  --color-background: {{ scheme.settings.background | color_extract: 'red' }}, {{ scheme.settings.background | color_extract: 'green' }}, {{ scheme.settings.background | color_extract: 'blue' }};
  {% if scheme.settings.background_gradient != blank %}
    --gradient-background: {{ scheme.settings.background_gradient }};
  {% else %}
    --gradient-background: rgba({{ scheme.settings.background | color_extract: 'red' }}, {{ scheme.settings.background | color_extract: 'green' }}, {{ scheme.settings.background | color_extract: 'blue' }}, 1);
  {% endif %}
  --color-button: {{ scheme.settings.button | color_extract: 'red' }}, {{ scheme.settings.button | color_extract: 'green' }}, {{ scheme.settings.button | color_extract: 'blue' }};
  --color-button-text: {{ scheme.settings.button_label | color_extract: 'red' }}, {{ scheme.settings.button_label | color_extract: 'green' }}, {{ scheme.settings.button_label | color_extract: 'blue' }};
  --color-link: {{ scheme.settings.secondary_button_label | color_extract: 'red' }}, {{ scheme.settings.secondary_button_label | color_extract: 'green' }}, {{ scheme.settings.secondary_button_label | color_extract: 'blue' }};

  background-color: rgb(var(--color-background)) !important;
  color: rgb(var(--color-foreground));
}

.multicolumn.color-scheme-{{ scheme_id }}.gradient {
  background: var(--gradient-background) !important;
}

/* Title and heading styling */
.multicolumn.color-scheme-{{ scheme_id }} .title {
  color: rgb(var(--color-foreground));
}

.multicolumn.color-scheme-{{ scheme_id }} .title-wrapper-with-link {
  color: rgb(var(--color-foreground));
}

.multicolumn.color-scheme-{{ scheme_id }} .underlined-link {
  color: rgb(var(--color-link));
}

.multicolumn.color-scheme-{{ scheme_id }} .underlined-link:hover {
  color: rgb(var(--color-button));
}

/* Button styling */
.multicolumn.color-scheme-{{ scheme_id }} .button--primary {
  background-color: rgb(var(--color-button));
  color: rgb(var(--color-button-text));
  border-color: rgb(var(--color-button));
}

.multicolumn.color-scheme-{{ scheme_id }} .button--primary:hover {
  background-color: rgb(var(--color-button));
  color: rgb(var(--color-button-text));
  opacity: 0.9;
}

/* Background style handling */
.multicolumn.color-scheme-{{ scheme_id }}.background-primary .multicolumn-card {
  background: rgb(var(--color-background)) linear-gradient(rgba(var(--color-foreground), 0.04), rgba(var(--color-foreground), 0.04));
}

.multicolumn.color-scheme-{{ scheme_id }}.background-none .multicolumn-card {
  background: transparent;
}

/* Link styling */
.multicolumn.color-scheme-{{ scheme_id }} .link {
  color: rgb(var(--color-link));
}

.multicolumn.color-scheme-{{ scheme_id }} .link:hover {
  color: rgb(var(--color-button));
}

/* Column content styling */
.multicolumn.color-scheme-{{ scheme_id }} .multicolumn-card__info h3 {
  color: rgb(var(--color-foreground));
}

.multicolumn.color-scheme-{{ scheme_id }} .multicolumn-card__info .rte {
  color: rgb(var(--color-foreground));
}

/* Slider navigation styling */
.multicolumn.color-scheme-{{ scheme_id }} .slider-button {
  background-color: rgb(var(--color-button));
  color: rgb(var(--color-button-text));
  border-color: rgb(var(--color-button));
}

.multicolumn.color-scheme-{{ scheme_id }} .slider-button:hover {
  background-color: rgb(var(--color-button));
  color: rgb(var(--color-button-text));
  opacity: 0.9;
}

.multicolumn.color-scheme-{{ scheme_id }} .slider-counter {
  color: rgb(var(--color-foreground));
}

/* Icon styling */
.multicolumn.color-scheme-{{ scheme_id }} .icon-wrap {
  color: inherit;
}

.multicolumn.color-scheme-{{ scheme_id }} .icon-wrap svg {
  fill: currentColor;
}
  {% endif %}
{% endfor %}
